export default function Settings() {
  return (
    <section className="page">
      <h1>Settings</h1>
      <p>Settings will be stored in the main process and exposed via IPC.</p>
    </section>
  );
}

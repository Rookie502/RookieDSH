export default function Agent() {
  return (
    <section className="page">
      <h1>Agent</h1>
      <p>Harness session entry will appear here once the runtime is running.</p>
    </section>
  );
}
